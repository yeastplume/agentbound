# Reference implementation

The reference implementation lives in [`crates/`](../crates/) (Rust workspace) and [`deploy/`](../deploy/) (catalogue, units, provisioning). This directory is retained only so older links resolve.

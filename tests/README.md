# Conformance and adversarial tests

This directory is reserved for reproducible tests mapped to the technical report's security invariants and the Phase 1 gates.

The suite will cover session interference, escape and persistence, credential recovery, gateway bypass, resource exhaustion, lifecycle fault injection, and audit reconstruction. Test results must distinguish prevention, detection, policy assumptions, and threat-model exclusions.

# Tests

The conformance driver and in-session probes live in [`crates/ab-conformance/`](../crates/ab-conformance/); evidence registers are under [`docs/evidence/`](../docs/evidence/). This directory is retained only so older links resolve.
